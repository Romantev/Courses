import './NavBar.scss'

function NavBar() {
    return (
        <nav className="navigation">
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Contact</a>
        </nav>
    )
}

export default NavBar;